%% sfCheck
%   outputs what the skew factor is supposed to be
%   and what the percent error is between the
%   calculated sf and what it should be
function [ output_args ] = sfCheck(pix_height, obj_height, dist, cam_height, cam_angle)
    
  obj_height = cam_height - (a - unskewed_h);
  sf = obj_height - cam_height
end